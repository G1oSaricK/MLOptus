# MLOptus

Project Description

Setup Instructions
1. Clone the repository:
    ```bash
    git clone git@github.com:NickOsipov/mloptus.git
    cd mloptus
    ```
2. Create a project:
    ```bash
    uv python pin 3.12.8
    uv init
    uv venv
    uv add --requirements requirements.txt
    ```
